# CODEX WIRE (`~/codex_monitor.py`) — 유저 친화 개선 일괄 작업

대상: `codex_monitor.py` (단일 파이썬 파일 대시보드, http://localhost:8787, `/api` 2초 폴링). **이 한 파일만 수정.** 기존 서빙/`/api` 구조·전반 비주얼 톤은 유지하되 아래를 전부 반영.

> ⚠️ 현재 이 모니터가 8787에서 **실행 중**일 수 있음. 파일만 수정하고 **실행 중 프로세스를 kill/restart 하지 말 것**(반영은 사용자가 재시작). 검증은 `python3 -m py_compile codex_monitor.py` 로 문법 통과 + 가능하면 snapshot 생성 함수 단위 self-test(서버 장기 실행 금지).

먼저 코드를 읽고 현재 수집/렌더 구조(ps 파싱·세션 JSONL 파싱·snapshot·/api·프론트 JS) 파악 후 작업.

---

## #0 (버그) 잡이 대시보드에 안 뜨는 문제 — 수정 [최우선] (근본원인 이미 진단됨)
**근본원인(확정)**: `codex_monitor.py:31` 의 `or "codex_monitor" in ln` 필터가 너무 넓다. `codex exec`는 프롬프트가 ps args에 통째로 들어가는데, 잡 프롬프트에 `~/codex_monitor.py` 문자열이 있으면 그 **잡 자체가 모니터 프로세스로 오인되어 `continue`로 제외**됨 → 그래서 codex_monitor를 분석하는 잡이 정확히 안 보였다.
**수정**: 라인 31의 문자열 전체 제외(`"codex_monitor" in ln`)를 없애고, **실제 서버 프로세스만 제외** — 즉 `python3 .../codex_monitor.py`로 *실행 중인 프로세스*(스크립트로 도는 것) 또는 `os.getpid()`/부모 pid만 제외. codex exec 잡의 프롬프트에 경로 문자열이 들어가도 안 걸리게.
**보조원인**: 완료/최근 세션 창이 좁음 — `codex_monitor.py:165` `all_sessions(10)` → **50 이상**, active 판정 `:186` `mtime<25s`·recent 보존을 별도 상수로 키워라(아래 #10과 통합). cwd 매칭(`:171` exact)·sandbox(`:40` 표시용)는 정상이라 건드리지 마라.
→ 결과: **모든 codex exec 잡이 sandbox/cwd/프롬프트 내용 무관하게 표시**.

## 개선 12종 (전부 반영)

1. **상태 구분 (running/완료/실패/좀비)**: pill을 상태별로. pid 살아있음 + 세션 mtime stale(임계, 예 >25s) = **좀비 경고**(노란/빨강), 정상 running=초록계, 완료=중립, 실패(비정상 종료/에러)=빨강. 아이콘/색 분리.

2. **잡별 출력/에러 상세 보기**: 세션 JSONL에서 `exec_command` 결과(stdout/stderr/exit)·최근 메시지를 수집해, 카드 클릭 시 **펼쳐서 최근 출력·에러 미리보기**. 실패 원인 파악용 stderr가 핵심.

3. **토큰·비용 집계**: 세션의 token usage(input/output/cache) 파싱 → 잡별 토큰 + 전체 누적. 대략 비용(모델 단가 상수로, gpt-5.5 가정값 주석) 표시. 기존 rate% 옆에.

4. **마지막 활동 시각(last-event age)**: 잡별 "마지막 이벤트 N초 전" 표시 → 멈춤/무응답 감지. 좀비 판정과 연동.

5. **카드 부분 갱신(재렌더 최소화)**: 2초 폴링 시 `R.innerHTML` 통째 교체 금지. 잡별 카드를 id로 추적해 **변경 필드만 in-place 업데이트**(없어진 잡 제거/새 잡 추가). 스크롤·접힘·클릭 상세 상태 보존.

6. **프롬프트 접기/펼치기**: 긴 프롬프트는 2~3줄로 클램프 + "더보기" 토글(내부 스크롤 96px 제거).

7. **필터·검색·정렬**: 상단 컨트롤바 — dir·sandbox·상태 필터, 텍스트 검색(프롬프트·파일명·PID), 정렬(경과시간/편집수/토큰/마지막활동).

8. **진행 단계 라벨**: 최근 이벤트 타입으로 현재 단계 추정(reading/analyzing/editing/verifying/idle) 표시.

9. **컴팩트 모드 + 반응형/원격**: 밀도 토글(컴팩트=한 줄 요약 리스트, 6개+ 한눈 스캔). 좁은 화면 반응형(min 380px·overflow-x:hidden 완화). 서버 bind 옵션 `--host 0.0.0.0`(기본은 127.0.0.1 유지, 플래그/환경변수로 LAN 공유 — 읽기전용이라 안전).

10. **완료 잡 히스토리 보존**: 완료 목록을 최근 9~10개 제한에서 늘려(예 50개) 장시간 병렬 후에도 추적. 메모리 캡만 두기.

11. **의미색 + 피드 소음 제어**: running/done/error **의미색 체계**로 정리(현 ember/rust 브랜드색과 분리). 글로벌 피드에서 edit/error/agent-message 등 **중요 이벤트 강조**, 반복 cmd 등 소음 약하게. 피드 길이도 적정선.

12. **새로고침 제어 + 잡 액션**: 폴링 주기 선택(1/2/5/10초) + 수동 새로고침 + "마지막 갱신 N초 전 / stale" 표시. **안전 액션**: 카드에 PID **kill** 버튼, 마지막 명령 **복사**, 같은 cwd/prompt **재시도**(재시도는 dispatch 래퍼 호출). → 액션은 `/api` 외 **POST 엔드포인트** 추가(로컬호스트 한정·확인 후 실행). kill은 `pkill -f "output-last-message <path>"` 식 안전 타깃.

---

## 마무리
- 단일 파일 유지. 기존 동작(기본 폴링·카드·피드) 깨지지 않게.
- `python3 -m py_compile ~/codex_monitor.py` 통과 필수.
- 끝나면 요약: #0 근본원인·수정, 12종 각 반영 위치(함수/라인), 새 POST 엔드포인트, 재시작 안내(사용자가 모니터 재시작해야 반영).
